# WeatherForescast

WeatherForecast is a api that has an integration with the [Open Meteo](https://open-meteo.com/en/docs) api to get a weather forecast.

## Notice

The motivation for creating this project was to build a solution on Linux, using VS Code and `dotnet` commands, completely stepping out of my comfort zone with Windows and Visual Studio 2026.
